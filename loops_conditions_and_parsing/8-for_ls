#!/usr/bin/env bash
# Lists files in the current directory and prints everything after the first '-'.
for file in *; do
	[ -e "$file" ] || continue
	case "$file" in
		.*) continue ;;
	esac
	echo "$file" | cut -d'-' -f2-
done
